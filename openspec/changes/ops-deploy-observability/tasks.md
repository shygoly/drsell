## 1. 部署清单

- [ ] 1.1 `deploy-mvp.sh` 生成 `/opt/drsell-run/deploy-manifest.json`：commit 短哈希、
      构建时刻、迁移头
- [ ] 1.2 清单含各应用**根 `.env` 与 standalone `.env`** 的关键项指纹（并排，D1/D2）
- [ ] 1.3 指纹为 sha256 前 12 位 + 长度；**清单里绝不含明文**
- [ ] 1.4 `check-deploy-facts` 断言脚本仍在生成清单——否则页面会静默显示旧值

## 2. API

- [ ] 2.1 `GET /api/ops/deploy` 读清单并合并运行时信息（pm2 进程启动时刻、重启次数）
- [ ] 2.2 清单缺失时返回明确的「未知」，不返回空对象让前端渲染成空白
- [ ] 2.3 合并 `WebhookSecretUse` 的代际证据（D4）与各店令牌到期
- [ ] 2.4 单测：指纹不一致时标记为异常
- [ ] 2.5 单测：清单缺失 → `unknown`，不抛错
- [ ] 2.6 单测：响应中不含任何明文密钥（用真实格式的假密钥断言不出现）

## 3. 运营台视图

- [ ] 3.1 `/deploy` 页：版本、配置指纹并排、密钥代际、令牌到期
- [ ] 3.2 指纹不一致高亮，并说明「进程读的是 standalone 那份」
- [ ] 3.3 令牌 24 小时内到期标记，并显示是否持有刷新令牌
- [ ] 3.4 进 superadmin 导航，走既有鉴权与审计

## 4. 收尾

- [ ] 4.1 `pnpm test`（含 `pnpm spec`）全绿
- [ ] 4.2 `openspec validate ops-deploy-observability --strict` 通过
- [ ] 4.3 `DEPLOY.md` 指向该视图——文档记「应该是什么」，视图记「现在是什么」
- [ ] 4.4 部署后走公网验证
