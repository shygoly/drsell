> **实施前有两个阻断项**（见 design 的 R2、D4/D5）：
> 1. `SHOPIFY_API_SECRET_PREVIOUS` 未收尾——`app_subscriptions/update` 仍靠旧密钥
>    才能验签通过。在一个会漏消息的通道上建停服判定，会停掉已付款的商家。
> 2. 两个产品决策未定：试用期额度（D2 待决）、按天触发方案（D4 三选一）。

## 0. 前置（不写业务代码）

- [ ] 0.1 收尾密钥：确认 Shopify 何时切到新密钥签 webhook，切换后删除 `SHOPIFY_API_SECRET_PREVIOUS`
- [ ] 0.2 定试用期额度：`@drsell/shared` 的 `PLANS` 目前没有试用档，D2 的实现依赖它
- [ ] 0.3 定按天触发方案（D4 的 a / b / c），b 应排除——没人访问就不提醒，而临近到期的店恰恰可能没人访问
- [ ] 0.4 核实生产上每个店的订阅镜像是否已与 Shopify 一致（`chatbotdomaintest` 的 `currentPeriodEnd` 仍停在 2025-08-25）

## 1. 可服务状态的判定（纯函数先行）

- [ ] 1.1 抽出 `isServiceable(subscription, now)`：`ACTIVE` / 试用中 / 宽限内 → true
- [ ] 1.2 宽限截止派生为 `currentPeriodEnd + 2d`，不落库（D3）
- [ ] 1.3 单测覆盖六个 Shopify 状态 × 试用中/外 × 宽限内/外的组合
- [ ] 1.4 单测：同一 `currentPeriodEnd` 反复判定，宽限截止不后移

## 2. 计费周期（改 `QuotaService`）

- [ ] 2.1 `periodStart` 锚点改为 `trialEnds`，试用期内不推进周期
- [ ] 2.2 移除「为失效订阅把周期推进到包含现在那一期」的行为，只对可服务订阅推进
- [ ] 2.3 `planCodeFor` 加状态筛选，不再拿作废订阅的档位当额度依据
- [ ] 2.4 单测：一年未续的订阅不再每 30 天重置额度
- [ ] 2.5 单测：同店多条订阅时取可服务的那条

## 3. 只读观测期（D6，先不拦截）

- [ ] 3.1 闸门判定结果只记录不拦截：记下「若开闸会拦谁、原因、订阅快照」
- [ ] 3.2 运营台能看到这份清单，便于人工核对是否误判
- [ ] 3.3 观察期结束、确认无误判后，才做第 4 步

## 4. 开闸（`AdpService`）

- [ ] 4.1 订阅闸门置于 `assertWithinQuota` **之前**（D1）
- [ ] 4.2 被拦下时顾客话术不暴露商家套餐/欠费/用量，会话转 `pending`
- [ ] 4.3 单测：订阅失效 + 额度也耗尽时，拦截原因记为订阅失效，且零上游调用
- [ ] 4.4 商家端显示停服原因与恢复入口，与「额度用尽」区分开

## 5. 到期提醒

- [ ] 5.1 按 0.3 选定的方案实现按天触发
- [ ] 5.2 D-3 / D-2 / D-1 各一次；不足三天时不补发已错过的档位
- [ ] 5.3 去重持久化，键为「店 + 周期 + 档位」，重启与多实例都不重复打扰
- [ ] 5.4 续费后针对旧周期的剩余提醒不再发出
- [ ] 5.5 应用内提示先落地（不依赖出站通道）
- [ ] 5.6 出站邮件：`sendDunning` 目前只排一条 `KnowledgeSyncJob` 给不存在的 worker，
      要么接上 `apps/api/src/mail/`，要么明确这条通道的归属
- [ ] 5.7 发送失败必须可被发现，不静默丢弃

## 6. 解冻

- [ ] 6.1 `app_subscriptions/update` 到达且状态转为可服务时，闸门随即放行
- [ ] 6.2 单测：`FROZEN` → 收到 `ACTIVE` 的 webhook → 无需人工即恢复
- [ ] 6.3 单测：webhook 迟到但仍在宽限窗口内时服务不中断

## 7. 收尾

- [ ] 7.1 `pnpm test`（含 `pnpm spec`）全绿
- [ ] 7.2 `openspec validate subscription-gating-and-expiry --strict` 通过
- [ ] 7.3 「订阅失效即停服」是产品语义的不可逆选择，结论入 `DECISIONS.md` 并在
      `ARCHITECTURE.md` 补论证；openspec 里只留指向该 ID 的引用
- [ ] 7.4 部署后走公网验证；观察期内确认没有商家被误停
