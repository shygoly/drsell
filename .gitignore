# Legacy stacks (separate repos — Java + old Remix chatbot)
/chatbot/
/chatbotadmin/
/chatbotapi/

# Dependencies & build
node_modules
dist
.next
.turbo
apps/api/dist
apps/web/.next

# Secrets & local env
.env
.env.local
.env.*.local
apps/*/.env
!apps/*/.env.example

# Certs & keys
*.pem
infra/docker/certs/*
!infra/docker/certs/.gitkeep

# Logs & OS
*.log
coverage
.DS_Store

# Prisma local db
apps/api/prisma/*.db

# IDE / tooling scratch
.playwright-mcp
.superpowers/sdd/progress.md
.serena/
.workbuddy/
_tmp_*

# Legacy Coze test scripts (pre-ADP monorepo)
test-coze-*.js

# TypeScript build info
*.tsbuildinfo

# 沙箱内使用的本地 pnpm store（避免写全局 store）
.pnpm-store

# stitch-to-shadcn-pro 产物：baseline.png 与 spec.json 是判定基准，必须入库；
# 其余每次 verify 都会重生成，不入库（合计 ~10MB）。
.stitch/reports/*/rebuilt.png
.stitch/reports/*/diff.png
.stitch/reports/*/textMask.png
.stitch/reports/*/boxes.json
.stitch/reports/*/axis1.json
.stitch/reports/*/axis2.json
.stitch/reports/*/diff.json
.stitch/reports/*/result.json
.stitch/reports/*/rebuilt.spec.json
.stitch/reports/*/pairing.json
.stitch/reports/*/fingerprint.json
.stitch/reports/*/report.html

# 新源阶段 A/C 产物：baseline/textMask/spec 是判定基准，入库；其余每次重生成
.stitch/project-11226504772808429506/work/reports/*/project.png
.stitch/project-11226504772808429506/work/reports/*/project-diff.png
.stitch/project-11226504772808429506/work/reports/*/boxes.json
.stitch/project-11226504772808429506/work/rebuild/
/opencode.json
.cursor/
.gstack/

# .stitch 设计基线整体不入库（2026-09-02 决定）：11.1MB 的截图/spec/基准只在本地保留。
# 注意：这与上面第 50 行「baseline.png 与 spec.json 必须入库」的旧决定相悖，以本条为准。
# 后果：scripts/stitch-*.sh|.mjs 依赖 .stitch/fixtures.json，clone 仓库者需自行获取该目录。
.stitch/
