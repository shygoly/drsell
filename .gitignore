# Legacy stacks (separate repos — Java + old Remix chatbot)
/chatbot/
/chatbotadmin/
/chatbotapi/

# Dependencies & build
node_modules
dist
.next
.turbo
apps/api/dist
apps/web/.next

# Secrets & local env
.env
.env.local
.env.*.local
apps/*/.env
!apps/*/.env.example

# Certs & keys
*.pem
infra/docker/certs/*
!infra/docker/certs/.gitkeep

# Logs & OS
*.log
coverage
.DS_Store

# Prisma local db
apps/api/prisma/*.db

# IDE / tooling scratch
.playwright-mcp
.superpowers/sdd/progress.md
.serena/
.workbuddy/
_tmp_*

# Legacy Coze test scripts (pre-ADP monorepo)
test-coze-*.js

# TypeScript build info
*.tsbuildinfo

# 沙箱内使用的本地 pnpm store（避免写全局 store）
.pnpm-store
