# Drsell 发布说明 — 2026-09-02（ops-stitch-alignment）

> 发布物：`main` 分支 merge commit `0c4601e`（ops-stitch-alignment 分支合入）。
> 部署目标：SSH host `wjclaw`，pm2 四进程，入口 `https://drsell.szchada.top` / `https://ops.szchada.top`。
> 关联文档：`docs/DRSELL_PUBLISH.md`（重上架清单）、`docs/superpowers/plans/2026-09-01-ops-stitch-alignment.md`（设计对齐计划）。

## 版本概览

| 项 | 值 |
|---|---|
| 发布名称 | ops-stitch-alignment（运营台 Stitch 深色对齐 + 代登录 + 审计） |
| 发布日期 | 2026-09-02 |
| Merge commit | `0c4601ee86721e573f99aa4a434a53e95c2f2ee7`（`0c4601e`） |
| 上一个 main 提交（回滚基线） | `baa4973`（2026-08-31） |
| 规模 | 36 commits，190 files changed，+214,181 / −129（大量新增为 `.stitch/` 设计基线与截图） |

一段话总结：本次发布把运营控制台（`apps/ops`，`ops.szchada.top`）按 Stitch 项目 `11226504772808429506` 的九屏 superadmin-dark 深色稿逐屏移植（accounts、shop detail bento 卡片、impersonation 会话视图、system、login、plans），API 侧新增 ops 模块的完整后端（superadmin guard、AuditLog 审计日志 + interceptor、订阅镜像、套餐目录）与**代登录 impersonation**（30 分钟 JWT）；Shopify embedded app 顶部新增 impersonation 提示横幅；店面聊天挂件（theme app extension）补齐颜色 fallback（未配置时回退品牌绿 `#006c49`）；新增 `/plans` 套餐页；部署脚本同步更新（ops vhost、Cloudflare DNS、pm2 `drsell-ops:5013`、健康检查）。

## 变更清单

### 1. Ops 控制台前端（`apps/ops`）

| 变更 | 说明 |
|------|------|
| Stitch 深色主题移植 | 按 stitch-to-shadcn 三阶段（A 静态锁定 → B 零漂移移植 → C 像素复核）逐屏对齐 superadmin-dark 稿；`tokens.css` 扩展色阶（amber scale、表面/边框层级），源码零 hex（`DS-7`） |
| 全局壳 | `app/components/shell.tsx`（搜索/通知/设置顶栏）+ `nav.tsx`（侧栏） |
| `/accounts` | 账号与会员总览（Stitch 02 布局），accounts 快照数据填充（`e8d5492`） |
| `/shops/[domain]` | 店铺详情 Stitch 08 bento 卡片，暴露 shop scopes、最近事件 |
| `/impersonation` | 代登录会话视图（Stitch 07 布局，逐字移植） |
| `/audit` | 审计日志页：筛选条 + 表格，失败行整行 `--tint-lost` |
| `/plans` | 套餐目录页（镜像 billing env 的 plan config） |
| `/system` | 系统页对齐 Stitch 05 |
| `/login` | 深色登录页（品牌块 + `INTERNAL ONLY`） |
| 主区背景修复 | main-area mismatch 67.4% → 15.4%（`49d215f`） |

### 2. API / 审计（`apps/api`）

| 变更 | 说明 |
|------|------|
| 新增 ops 模块 | `src/ops/`：`ops.controller.ts`（`/ops/me`、`/ops/plans`、`/ops/queue`、`/ops/accounts*`、`/ops/shops*`、`/ops/audit-logs`）、`ops.service.ts`、`ops.module.ts` |
| 权限 | `ops-superadmin.guard.ts`：仅 `typ=admin && role=superadmin` 放行 |
| 审计日志 | Prisma `AuditLog` model + migration `20260902090000_audit_log`；`audit.interceptor.ts` + `@Audit` decorator + `ops-audit.util.ts`，记录 actor/action/shopDomain/payload/result/ip |
| 代登录 impersonation | `POST /ops/shops/:domain/impersonate`：签发 `impersonation: true` 的 JWT，`expiresIn 30min`，记录 `impersonatedBy`，带 `@Audit` 审计 |
| 写操作端点 | dunning / extend-freeze / billing-shop / resync / disable-widget / enable-widget（均审计） |
| 订阅镜像 | `subscription-mirror.service.ts`：ops 侧订阅状态镜像 |
| 套餐目录 | `ops-plan.config.ts`：`free`（500 chats/200 AI/1 seat）与默认 `pro`=Growth（$79、3000 chats、1000 AI、5 seats、超额 $0.9、14 天试用），可经 `OPS_PLAN_*`/`BILLING_*` env 覆盖 |
| 其他 migration | `20260902093000_chat_stat_ai_resolved`（今日对话统计 AI 解决数字段） |
| 小修 | `shopify.service.ts`、`storefront-dashboard.service.ts`、`billing.service.ts`、`subscription.service.ts`、`adp.service.ts` |

### 3. Embedded App（`apps/web`）

| 变更 | 说明 |
|------|------|
| impersonation 接入 | `hooks/useShopSession.ts`：识别 URL `?impersonation_token=` → 存 localStorage → 解码 JWT 判定 `impersonation: true` → 从地址栏清除参数 |
| 提示横幅 | `app/app/client-layout.tsx`：代登录态下顶部渲染 `impersonation-banner`（`role="status"`），样式见 `app/globals.css` |

### 4. 主题挂件（theme app extension，`apps/web/extensions/chatbot`）

| 变更 | 说明 |
|------|------|
| 颜色 fallback | `assets/drsell-chat.js` 新增 `FALLBACK` 色板（品牌绿 `#006c49` 等 7 色）与 `primaryColor()` / `headerColor()` 回退链：`config.widget*Color` 未配置时逐级回退，气泡、launcher、输入框边框统一走 fallback，不再出现 undefined 色值 |

### 5. Infra / Deploy

| 变更 | 说明 |
|------|------|
| ops vhost | `infra/nginx/ops.szchada.top.conf`：8088（Cloudflare origin 回源，plain HTTP）+ 8443 ssl；`/api/` → 127.0.0.1:5011，`/` → 127.0.0.1:5013（带 WebSocket upgrade），`/healthz` 返回 ok |
| Cloudflare | 新增 `infra/cloudflare/configure-ops-dns.sh`（ops.szchada.top proxied A），`wrangler.toml` / `upsert-dns.sh` 更新 |
| 部署脚本 | `scripts/deploy-mvp.sh`：新增 ops 构建 + standalone rsync + `pm2 drsell-ops:5013`、ops vhost 同步、清理旧 `ops.drsell.szchada.top.conf`、缺失时 openssl 自签 ops 证书、四进程健康检查 |
| 设计基线 | `.stitch/`（spec/reports/rebuild/designs）、`scripts/stitch-*.sh|.mjs`、`spec/check-design.mjs`、`spec/check-ops-*.mjs`、`spec/negative-verify.mjs` 入库 |

## 部署流程

### 前置条件

1. SSH 免密可达 `wjclaw`（`ssh wjclaw true` 应无输出退出）。
2. 本地 env 文件齐备：
   - `apps/api/.env` — `DATABASE_URL`、`SHOPIFY_API_KEY`/`SHOPIFY_API_SECRET`、`JWT_SECRET`（生产必须改掉 `drsell-dev-secret-change-me`）；
   - `apps/web/.env` — `SHOPIFY_API_KEY`、`NEXT_PUBLIC_SHOPIFY_API_KEY`；
   - 可选：`apps/storefront/.env`、`apps/ops/.env`（构建期注入 `NEXT_PUBLIC_API_URL`，缺省 `https://ops.szchada.top/api`）。
3. 本地 pnpm 可用；无需 Docker（wjclaw 不做远端构建）。

### 执行

```bash
bash scripts/deploy-mvp.sh
```

脚本按顺序做以下事情：

1. **本地构建**：packages（shared/shopify/adp/openclaw）→ `prisma generate` → api → web（standalone）→ storefront → ops。
2. **rsync 产物** 到 `wjclaw:/opt/drsell-run`（api dist + prisma/、packages dist、三个 Next standalone + static + public；同步 `apps/api/.env`、`apps/web/.env`、`apps/storefront/.env`）。
3. **nginx vhost 同步**：drsell + ops conf 推到 `/opt/webrtc-ws-proxy/conf.d/`，删除旧 `ops.drsell.szchada.top.conf`，无 ops 证书时自签。
4. **远端安装 + 迁移**：修正远端 `.env`（`PORT=5011`、`API_INTERNAL_URL=http://127.0.0.1:5011`、`postgres:5432`→`127.0.0.1`）→ `pnpm install --prod` → `prisma@6.9.0 migrate deploy` + `generate`（本次会应用 `20260902090000_audit_log`、`20260902093000_chat_stat_ai_resolved`）。
5. **pm2 重启**（delete + start + save）：

| pm2 进程 | 端口 | 说明 |
|---|---|---|
| `drsell-api` | 5011 | NestJS API |
| `drsell-storefront` | 5010 | 公开店面 |
| `drsell-web` | 5012 | Shopify embedded app |
| `drsell-ops` | 5013 | 运营控制台（新增） |

6. **nginx reload**：`docker exec webrtc-ws-proxy nginx -t && nginx -s reload`。
7. 清理旧备份 conf；（可选，需 `CF_API_TOKEN`）执行 `configure-ops-dns.sh` 保证 ops DNS 为 proxied A。

### 部署即验证（脚本内置健康检查）

| 命令 | 期望 |
|---|---|
| `curl -sf http://127.0.0.1:5011/api/health` | 返回 ok/JSON，exit 0 |
| `curl -o /dev/null -w "%{http_code}" http://127.0.0.1:5010/` | 200 |
| `curl -o /dev/null -w "%{http_code}" http://127.0.0.1:5012/app` | 200 |
| `curl -o /dev/null -w "%{http_code}" http://127.0.0.1:5013/login` | 200 |
| `curl -s https://drsell.szchada.top/api/health`（经 Cloudflare） | 200 |
| `curl -s https://ops.szchada.top/healthz` | `ok` |

## 发布后冒烟测试清单（已执行）

执行人：Claude Opus 5（全自动，无人工介入）　执行时间：2026-09-03　环境：生产（Cloudflare → wjclaw）

**结果：PASS 26 · FAIL 0 · BLOCKED 2**（首轮 PASS 20 / FAIL 2 / BLOCKED 2，两个 FAIL 已定位根因并修复，复测转 PASS）

登录授权全程自动化，未使用任何人工凭据输入：superadmin 凭据从服务器 `apps/api/.env` 的
`BOOTSTRAP_ADMIN_*` 读取 → `POST /api/auth/admin/login` 换 JWT → 后续全部带 Bearer。

### A. 店面与 Embedded App（`https://drsell.szchada.top`）

| 结果 | 检查项 | 证据 |
|------|--------|------|
| PASS | 店面首页 `/` | 200，28074B，`<title>AIChat Merchant Dashboard</title>`，导航/主区/CSS 齐全 |
| PASS | API 健康 `/api/health` | 200 `{"ok":true,"service":"drsell-api"}` |
| PASS | Embedded App 入口 `/app` | 200 `<title>Drsell</title>`（**修复后**；修复前返回 storefront，与 `/` 响应 md5 完全相同） |
| PASS | `/app` 静态资产 | 13/13 全 200（**修复后**；修复前 `/app/_next/*` 全部 404，页面在浏览器里是死的） |
| PASS | 挂件颜色 fallback（源码） | `drsell-chat.js` 内 `FALLBACK.primary='#006c49'` 三层回退链，6+2 处调用 |
| PASS | 线上挂件资产 | `/drsell-chat.js` 200，含 `#006c49`×8 |
| PASS | AI 对话链路（OpenClaw 直连） | wjclaw gateway SSE 7 chunk 拼出 `SMOKE_OK`，`finish_reason:stop` |
| PASS | AI 对话链路（生产全链路） | `POST /api/public/chat` 201，`text/event-stream`，4.15s 返回 |
| PASS | 对话落库 | `chatCount 16→17`，`aiResolved 0→1` |
| PASS | pm2 日志 | 当前进程 error 零条目 |
| BLOCKED | 挂件在真实主题店前台 | 需已装挂件的测试店 + `shopify app deploy`，后者待 Partner token |
| BLOCKED | 店面浏览器 console 报错 | 浏览器工具 navigate 300s 超时，playwright MCP 亦 CONNECT_TIMEOUT |

> OAuth 安装流与 Webhooks 未列入本轮：需要商户店铺登录，属人工范畴；现有已装店
> `chatbotdomaintest.myshopify.com` 的 access token 有效，代登录链路已借它验证。

### B. 运营控制台（`https://ops.szchada.top`）

| 结果 | 检查项 | 证据 |
|------|--------|------|
| PASS | 健康检查 `/healthz` | 200，body `ok` |
| PASS | superadmin 登录 | 201，JWT `role=superadmin` `typ=admin` |
| PASS | `/api/ops/me` | 200，身份正确（仅回 email，无 role 字段） |
| PASS | `/api/ops/plans` | Growth `$79 / 3000 chats / 1000 AI / 5 seats / 14 天试用` + Free，与本文档逐字一致 |
| PASS | `/api/ops/queue` | 200 `[]` |
| PASS | `/api/ops/accounts` | `?q=admin`、`?q=sgl` 均命中（空 q 返回 `[]` 是设计） |
| PASS | `/api/ops/audit-logs` | 200，结构正确 |
| PASS | 店铺列表与详情 | 详情含 scopes/用量/套餐/installedAt 共 24 字段 |
| PASS | 代登录签发 | `impersonation:true`、`exp-iat=1800`、`impersonatedBy` 三项全中 |
| PASS | 代登录落地 | 带 token 打开 `/app` 返回 Drsell 且资产全通（**修复后**） |
| PASS | 审计闭环 | `total` 递增，`shop.impersonate`/actorEmail/`result:ok` 全对 |
| PASS | 越权：无 token | 401 |
| PASS | 越权：伪造 token | 401 |
| PASS | 越权：真实 `role=owner` 账号 | 6 端点全 403 `superadmin required`；同密钥 superadmin 对照组 200 |
| PASS | 越权：shop 态 token | 403 `superadmin required` |
| PASS | ops 前端 7 页 | `/login` 200；其余 307 → `/login?next=<原路径>` |

### 本轮发现并修复的生产故障

**代登录端到端不可用**——本次发布的头号功能实际从未在生产生效。两层根因：

1. `infra/nginx/drsell.szchada.top.conf` 的 `location ^~ /app` rewrite 后代理到
   storefront(5010)，而 impersonation 代码在 apps/web(5012)。该 block 里留着
   App Bridge 的 `sub_filter`，证明它本就是为 embedded app 写的，upstream 指错了。
2. 改对 upstream 后暴露更深根因：apps/web 与 apps/storefront 共用同一域名根，
   两者静态资产都在 `/_next/`，而 `location /` 把 `/_next/*` 全送给 storefront，
   apps/web 的 chunk 一律 404。

修法见提交 `2be379d`：apps/web 加 `assetPrefix`（`ASSET_PREFIX` 驱动），资产隔离到
`/app/_next/`；nginx 加更具体的 `location ^~ /app/_next/` 剥前缀回源 5012。

**为什么部署自检没抓到**：`deploy-mvp.sh` 用 `curl 127.0.0.1:5012/app` 直连上游、
绕过 nginx，故永远是绿的。且这两个故障都是「200 但内容错」，纯状态码断言覆盖不到。
**待办：健康检查应改走公网域名并断言内容特征。**

### 其他观察（非 FAIL，未处置）

- `AuditLog.ip` 记成 `127.0.0.1`（nginx 回源地址），审计无法溯源真实 IP
- `searchAccounts` 用 `startsWith`，搜 `gmail` 命中不了 `sgl1226@gmail.com`
- 存在陈旧账号 `admin@drsell.szchada.com`（疑为 `.top` 笔误遗留的多余 superadmin）
- 两份 `drsell-chat.js` 已漂移 533 字节（storefront 副本无 FALLBACK 对象）
- 一个历史故障已消解：`listAuditLogs` 的 `prisma.auditLog undefined`（migration 跑了
  但 Client 未 generate），本次部署已修复，实测正常


## Shopify 插件发布清单

目标 App：**Drsell**，App ID **264501002241**，client_id `0b36b70772220b71b2fe296b3deba914`，handle `drseller-alpha`，所属 Partner：苏州畅达；当前状态 **Delisted**（需重新过审上架）。

### 已核对完成（2026-09-03，全自动）

| 项 | 结论 |
|---|---|
| client_id 一致性 | ✅ **五处全一致**：`apps/web/shopify.app.toml`、本地 `apps/web/.env`、本地 `apps/api/.env`、服务器 `/opt/drsell-run/apps/{web,api}/.env`。线上 OAuth 实跑回显的 authorize URL 里 client_id 与 redirect_uri 亦对得上 |
| Shopify CLI | ✅ 已装 `@shopify/cli 4.7.1`（workspace root），`npx shopify version` 可跑。未执行任何 login/deploy |
| URL 可达性 | ✅ `/`、`/api/health`、`/api/auth/callback`、`/api/webhooks`（POST 无 HMAC→401、GET→405）行为全部正确 |
| App icon | ✅ 已重导为合规版 `design/brand/icon-store-1200.png`（1200×1200、RGB、无 alpha、logo 四边留白 21.9–26.6%）。**上传 listing 用此文件，不要用 `icon-1200.png`** |
| 色差 | ✅ `#4FA84B` vs Shopify 亮绿 `#95BF47` ΔE2000=12.40（色相差 36°）、vs 深绿 `#5E8E3E` ΔE=8.82（21°）。同族但明显可辨，构图亦无相似，不构成仿冒风险 |

### 部署前必须先改（否则会破坏 Partner 侧配置）

1. **`shopify app deploy` 会把整个 toml 覆盖到 Partner 后台**（CLI 4.7.1 起
   `include_config_on_deploy` 废弃，配置强制随 deploy 上传）。因此：
   - toml 里 `name = "Dr Sell"` 会把 Partner app **改名**（现为 "Drsell"）→ 先改成 `Drsell`
   - `handle = "drseller-alpha"` 会**覆盖现有 listing slug** → deploy 前先在后台确认现有 handle
2. **`redirect_urls` 里有两条死链**：`/auth/callback`、`/auth/shopify/callback` 实测均 404，
   `apps/web/app/` 下没有这两条路由 → 从 toml 删掉
3. **必须带 `--path apps/web`**。仓库根目录没有 toml，而 `chatbot/shopify.app.toml:3`
   是一份**可用的 legacy jade app 配置**（`f286a4af8f1d80cb8e6228bc648f4786`）——
   谁要是 `cd chatbot` 跑 deploy 就会把 legacy app 发出去。
4. `--force` 在 CLI 4.7.1 **不存在**，已拆成 `--no-release` / `--allow-updates` /
   `--allow-deletes`。**`--allow-deletes` 绝对不要用**（会静默删掉 Partner 侧的
   dashboard-managed extension）。

### 两个卡审的硬缺口（必须先补）

| 缺口 | 现状 | 影响 |
|---|---|---|
| **隐私政策页不存在** | `/privacy`、`/privacy-policy`、`/legal/privacy`、`/terms` 实测全 404（`/support` 200 可用） | listing 的隐私政策 URL 栏没有可填地址，无法提审 |
| **GDPR webhook 是空实现** | `shopify.controller.ts:203` 里 `customers/redact`、`shop/redact` 在 HMAC 校验通过后一律 `return {ok:true}`，未真实删数据 | 自动探测会过，人工审可能追问 |

### 执行步骤

| ☐ | 步骤 | 操作 | 谁做 |
|---|------|------|------|
| ☐ | 1. 补隐私政策页并上线 | 内容需产品确认 | 人工定内容 |
| ☐ | 2. 改 toml：`name=Drsell`、删两条死 redirect | 见上「部署前必须先改」 | 可自动 |
| ☐ | 3. 后台确认现有 handle | Partner Dashboard | **人工** |
| ☐ | 4. `--no-release` 建版本 | `npx shopify app deploy --path apps/web --client-id 0b36... --no-release` | 需 CLI token |
| ☐ | 5. 复核版本 diff | 确认未误改 name/handle/extension | **人工** |
| ☐ | 6. 发布版本 | `npx shopify app release --path apps/web --version <名>` | 需 CLI token |
| ☐ | 7. 上传 icon（`icon-store-1200.png`）、listing 文案、3–6 张 1600×900 截图、隐私政策 URL、测试店铺账号 | Partner Dashboard | **人工** |
| ☐ | 8. Submit for review | Distribution → Submit | **人工** |
| ☐ | 9. 过审后归档 legacy jade app | 消除误用风险 | **人工** |

> **严禁**：legacy jade app `client_id=f286a4af8f1d80cb8e6228bc648f4786` 不得用于生产。
> 注意 `docs/PROJECT_ANALYSIS.md:204/331` 记录着它的 **secret 明文且已入 git 历史**
> （仓库为 private，暴露面限于有仓库权限者，仍建议轮换）。

## 回滚方案

**前提认知**：`deploy-mvp.sh` 用 `rsync --delete` 直接覆盖 `wjclaw:/opt/drsell-run` 下产物，服务器上**没有历史版本**；回滚只能回退到 git 提交后重新部署。

### 应用回滚（git + 重新部署）

1. 确定回滚目标：本次发布前基线为 `baa4973`（或 `git log --oneline main` 自选）。
2. 在本地仓库回退并重新部署：

```bash
git checkout baa4973        # 或 git revert 0c4601e（生成反向提交）
bash scripts/deploy-mvp.sh
```

3. 脚本本身会 delete + start 四个 pm2 进程，等效 `pm2 restart`；如只需重启某进程：`ssh wjclaw "pm2 restart drsell-ops"`。
4. 验证：重复上文「部署即验证」六条健康检查。

### 数据库回滚

- 本次两个 migration（`20260902090000_audit_log`、`20260902093000_chat_stat_ai_resolved`）均为**纯新增**表/字段，旧代码不感知，**回滚代码无需回滚 DB**。
- 确需回滚 DB 时在 wjclaw `apps/api` 下用 `prisma migrate resolve --rolled-back <name>` 标记并手工 drop，务必先 `pg_dump` 备份。

### nginx 回滚

- vhost 由脚本覆盖至 `/opt/webrtc-ws-proxy/conf.d/`；建议部署前手工备份：
  `ssh wjclaw "cp /opt/webrtc-ws-proxy/conf.d/ops.szchada.top.conf{,.bak.$(date +%F)}"`
- 回滚：恢复备份 conf → `docker exec webrtc-ws-proxy nginx -t` → `nginx -s reload`。
- Cloudflare：`configure-ops-dns.sh` 只新增 ops 的 proxied A 记录；回滚 ops 域名时在 CF 面板删除该记录即可。

## 已知限制与后续事项

> 本节已按 2026-09-03 实测结果修订，删除了已不成立的条目。

1. **两个并行 embedded app 未收口（架构决策待定）**：`apps/web`（5012，真正的 Shopify
   embedded app，含 impersonation）与 `apps/storefront`（5010，title 为 "AIChat Merchant
   Dashboard"）职责重叠，共用同一域名根。本次已用 `assetPrefix` 隔离静态资产止血，但
   **该留哪个、另一个去哪，需要产品决策**。
2. **部署健康检查形同虚设**：`deploy-mvp.sh` 用 `curl 127.0.0.1:5012/app` 直连上游绕过
   nginx，本次两个生产故障它全程绿灯。应改为走公网域名 + 断言内容特征（如 `<title>`），
   纯状态码断言覆盖不了「200 但内容错」。
3. **Shopify 上架两个硬缺口**：隐私政策页不存在（全 404）、GDPR webhook（`customers/redact`、
   `shop/redact`）是空实现未真实删数据。详见上文 Shopify 段。
4. **Stitch 对齐未收口**：新九屏源的像素复核仍有差距（masked diff：accounts 34.62%、
   system 65.56%、impersonation 21.59%、shop_detail 17.05%，红=0）。
   **注意**：`.stitch/` 已于 2026-09-03 整体移出版本库（见提交 `2b74dcf`），11.1MB 设计
   基线仅本地保留；这与 `.gitignore` 第 50 行「baseline.png 与 spec.json 必须入库」的旧
   决定相悖，以新决定为准。后果：`scripts/stitch-*.sh|.mjs` 依赖的 `.stitch/fixtures.json`
   随之出仓，clone 仓库者需自行获取该目录才能跑设计回归。
5. **套餐目录单一**：目前仅 `free` + 单一 Growth 档（env 可调），多档套餐待产品决出。
6. **Impersonation 无主动吊销**：30 分钟自然过期，无「提前踢出」机制；如需强制下线要补
   服务端黑名单/会话表。
7. **审计无法溯源真实 IP**：`AuditLog.ip` 记的是 nginx 回源地址 `127.0.0.1`，应改用
   `X-Forwarded-For`。
8. **`searchAccounts` 用 `startsWith`**：搜 `gmail` 命中不了 `sgl1226@gmail.com`，
   应改子串匹配。
9. **陈旧 superadmin 账号**：`admin@drsell.szchada.com` 疑为 `.top` 笔误遗留，多一个
   superadmin 是权限面风险，建议清理。
10. **两份 `drsell-chat.js` 已漂移 533 字节**（storefront 副本无 FALLBACK 对象），
    需定一份为准。
11. **ops 证书**：8443 用 openssl 自签，正常流量走 Cloudflare → 8088 plain HTTP 回源；
    若未来直连 8443 需换正式证书。
12. **`docs/PROJECT_ANALYSIS.md` 含 legacy app secret 明文且已入 git 历史**：仓库为
    private，暴露面有限，仍建议轮换并归档 legacy app。
13. **`opencode.json` 含明文 Google Stitch API key**：已确认被 gitignore、无历史泄漏；
    若该文件曾以其他方式分享过，建议轮换。
14. **项目根 `CLAUDE.md` 已过期**：通篇按 Coze 集成描述架构，实际 AI 链路早已改为
    wjclaw 上的 OpenClaw（`packages/openclaw` → gateway :18790，provider DeepSeek-V4）。
    实测 `coze` 在 `apps/`+`packages/`+`scripts/`+`infra/` 零命中。待修订。
15. 今日对话统计的 AI 解决数已加字段（`chat_stat_ai_resolved`），前端展示口径需在下一
    迭代对齐。

## 本次发布实际执行记录（2026-09-03）

| 动作 | 结果 |
|---|---|
| `ops-stitch-alignment` 合并入 main | 已完成（`0c4601e`） |
| `.stitch` 移出版本库 | 88 文件 / 11.1MB 从待推历史剔除（`git filter-branch --index-filter`，仅重写未推送范围），本地文件完整保留，备份分支 `backup/pre-stitch-strip` |
| 生产故障修复 | `/app` 路由与静态资产撞车，代登录恢复可用（`2be379d`） |
| 冒烟测试 | PASS 26 / FAIL 0 / BLOCKED 2，全自动无人工介入 |
| GLM-5.3-Flash 开发侧编排 | `tools/glm-swarm/`（`766e507`），离线自测 78 项通过，待 `GLM_API_KEY` 实跑 |
| OpenClaw | **保持 DeepSeek-V4 不变**，本次未做任何改动 |
| Shopify 重新上架 | 就绪度已核对完毕，待 Partner CLI token 与人工补隐私政策页 |
