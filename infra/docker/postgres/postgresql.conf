# Listen on all interfaces inside container; host publishes 127.0.0.1:5433 only
listen_addresses = '*'
port = 5432
ssl = on
ssl_cert_file = '/certs/server.crt'
ssl_key_file = '/certs/server.key'
password_encryption = scram-sha-256
max_connections = 100
